RewriteEngine On

# Deny direct access to config and storage
RewriteRule ^(config|storage)/ - [F,L]

RewriteCond %{REQUEST_FILENAME} !-f
RewriteCond %{REQUEST_FILENAME} !-d
RewriteRule ^ index.php [L]
